[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.30.2"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell", "powershell", "homebrew", "msi"]
# A GitHub repo to push Homebrew formulas to
tap = "bgreenwell/homebrew-tap"
# A GitHub repo to push Scoop manifests to
scoop-bucket = "bgreenwell/scoop-bucket"
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu", "x86_64-unknown-linux-musl", "x86_64-pc-windows-msvc"]
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Publish jobs to run in CI
publish-jobs = ["homebrew"]
# Note: Scoop publishing is handled by separate workflow (publish-scoop.yml)
# Note: AUR publishing is handled by separate workflow (publish-aur.yml)
# Note: crates.io publishing is automated via publish-crates-io job in release.yml
# Whether to install an updater program
install-updater = true
